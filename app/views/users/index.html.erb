<% provide(:title, 'All users') %>
<h1>All Users</h1>

<%= will_paginate @users %>

<ul class="users">
    <% @users.each do |user| %>
        <%= render user %>
    <% end %>
</ul>

<%= will_paginate @users %>